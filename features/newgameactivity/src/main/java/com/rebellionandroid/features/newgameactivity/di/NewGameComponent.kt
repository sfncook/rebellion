package com.rebellionandroid.features.newgameactivity.di

import com.rebllelionandroid.core.di.scopes.FeatureScope
import dagger.Component

@FeatureScope
@Component(
//    modules = [AppComponent::class]
)
interface NewGameComponent {

    /**
     * Inject dependencies on component.
     *
     * @param detailFragment Detail component.
     */
//    fun inject(detailFragment: CharacterDetailFragment)
}
